export function selectField(gameState) {
  return gameState.field;
}

export function selectDirection(gameState) {
  return gameState.direction;
}

export function selectSnakeSize(gameState) {
  return gameState.snakeSize;
}
